<!-- pricing section starts  -->
<section class="pricing services" id="pricing">
    <h1 class="heading">Lista<span> Personal</span> </h1>
    <div class="box-contanier">

        <div class="box">
            <span class="fas fa-users"></span>
            <div class="container">

                <a href="javascript:history.back()" class="btn">Volver</a>
            </div>

            <h3>Todos los Registros</h3>

            <table class="resp">
                <thead>
                    <tr class="tablacolor">
                        <th scope="col">
                            <h1>N.</h1>
                        </th>
                        <th scope="col">
                            <h1>Nombre</h1>
                        </th>
                        <th scope="col">
                            <h1>Fecha de Ingreso</h1>
                        </th>
                        <th scope="col">
                            <h1>Estatus</h1>
                        </th>
                        <th scope="col">
                            <h1>Operacion</h1>
                        </th>
                    </tr>
                </thead>
                <tbody>

                    {{#each user}}

                    <tr>
                        <th scope="row">{{@index}}</th>
                        <td>
                            {{name}}
                        </td>

                        <td>
                            {{createdAt}}
                        </td>

                        <td>{{#if status}}
                            Activo
                            {{else}}
                            Inactivo
                            {{/if}}
                        </td>
                        <td>
                             <a href="/personal/info/{{_id}}" class="btn"><i class="fas fa-id-card-alt"></i></a>
                            <a href="/personal/notes/{{_id}}" class="btn"><i class="fas fa-file-alt"></i></a>
                            {{#if status}}
                            <a href="/personal/toogleStatus/{{_id}}" class="btn"><i class="fas fa-thumbs-down"></i></a>
                            {{else}}
                            <a href="/personal/toogleStatus/{{_id}}" class="btn"><i class="fas fa-thumbs-up"></i></a>
                            {{/if}}
                        </td>
                    </tr>

                    {{/each}}

                </tbody>
            </table>


        </div>


    </div>
</section>
<!-- pricing section ends -->